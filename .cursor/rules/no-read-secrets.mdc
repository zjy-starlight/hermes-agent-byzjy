---
description: 未经用户明确要求时，不读取 .env 与 ~/.hermes 密钥文件
alwaysApply: true
---

# 密钥与本地私密配置（强制）

在本仓库对话中，**除非用户明确写出要读取、粘贴或排查某个具体路径/文件**，否则：

1. **不要**使用读取工具打开、搜索或展示以下内容：
   - 任何名为 `.env`、`.env.*` 的文件内容
   - 用户主目录下 `~/.hermes/.env`、`%USERPROFILE%\.hermes\.env` 及同目录下明显用于密钥的文件
   - 其它用户指明的「仅密钥」路径

2. **可以**做的事：
   - 仅根据官方文档 / 代码里的 `OPTIONAL_ENV_VARS`、配置说明，告诉用户**应配置哪些变量名**、类型与示例占位符（如 `sk-...`），**不**索要或读取真实值。

3. 若用户说「帮我看 .env / 排查密钥」，视为**明确授权**，此时再按需读取其**指定路径**（仍避免在回复中全文复述密钥，可只说明结构或脱敏）。
